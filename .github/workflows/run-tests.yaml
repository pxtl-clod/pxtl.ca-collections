name: Run Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            10.x
            8.0.x

      - name: Restore dependencies
        run: dotnet restore PxtlCa.Collections.slnx --verbosity normal

      - name: Build library and tests
        run: dotnet build PxtlCa.Collections.slnx --no-restore --configuration Release -p:TreatWarningsAsErrors=false

      - name: Build solution
        run: dotnet build PxtlCa.Collections.slnx --no-restore --configuration Release

      - name: Run tests
        run: dotnet test PxtlCa.Collections.slnx --no-build --configuration Release --verbosity normal